<Window x:Class="SynoCamWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:SynoCamWPF.ViewModels"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        Title="MainWindow" Height="171" Width="525" Topmost="True" AllowsTransparency="True"
        WindowStyle="None" Background="Transparent" Loaded="Window_Loaded" ResizeMode="CanResizeWithGrip"
        MouseDown="MainWindow_OnMouseDown">
    <Window.DataContext>
        <viewModels:MainWindowViewModel />
    </Window.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseEnter" >
            <i:InvokeCommandAction Command="{Binding ShowWindowControls}" />
        </i:EventTrigger>
        <i:EventTrigger EventName="MouseLeave" >
            <i:InvokeCommandAction Command="{Binding HideWindowControls}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <ItemsControl Background="Black" x:Name="CamPanel" ItemsSource="{Binding CameraViews, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"></StackPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding Url, IsAsync=True}" MaxHeight="{Binding RelativeSource={RelativeSource AncestorType={x:Type StackPanel}}, Path=ActualHeight}" 
                           MaxWidth="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=ActualWidth, Converter={StaticResource DivideByTwo}}"
                           Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=ActualWidth, Converter={StaticResource DivideByTwo}}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        <StackPanel HorizontalAlignment="Right"  x:Name="ControlPanel" Orientation="Vertical" Width="15" Background="Transparent">
            <Button x:Name="CloseButton" Visibility="{Binding WindowControlClose}" Background="Black" Foreground="White" BorderThickness="0" Opacity="0.7">X</Button>
            <Button x:Name="MinimizeButton" Visibility="{Binding WindowControlMinimize}" Background="Black" Foreground="White" BorderThickness="0" Opacity="0.7">-</Button>
            <Button x:Name="RefreshButton" Visibility="{Binding WindowControlRefresh}" Background="Black" Foreground="White" BorderThickness="0" Opacity="0.7">R</Button>
        </StackPanel>
    </Grid>
</Window>
